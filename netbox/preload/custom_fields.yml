biome:
  type: select
  label: Biome
  description: A user-defined network zone (e.g. IT, OT, DMZ) used to group devices for connection mapping.
  required: false
  filter_logic: exact
  weight: 30
  is_cloneable: true
  on_objects:
  - dcim.models.Device
  - virtualization.models.VirtualMachine
  choice_set:
  - [it, IT]
  - [ot, OT]
  - [dmz, DMZ]
